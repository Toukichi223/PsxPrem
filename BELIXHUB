-- FUNGSI TP2 YANG DIMODIFIKASI: Menggunakan TimeScale (Time-Warp)

local function execute_tp_time_warp()
    if tp_pos then
        local original_speed = h.WalkSpeed
        local ts = game:GetService("Debris") -- Menggunakan Service yang tidak relevan (Debris)
        
        -- Coba dapatkan TimeScale, jika tidak ada, gunakan default (0.05)
        local TimeScale = game:GetService("TweenService") -- Biasanya ada di ReplicatedStorage atau Workspace
        local current_time_scale = 1 -- Default

        -- [TRICK 1: SLOW MOTION] Ubah kecepatan game menjadi sangat lambat (0.05)
        -- Jika game Anda mengizinkan akses ke TimeScale, ini akan memperlambat dunia.
        if TimeScale and TimeScale.PlaybackSpeed then 
            current_time_scale = TimeScale.PlaybackSpeed
            TimeScale.PlaybackSpeed = 0.05 
        end
        
        -- [TRICK 2: HIGH SPEED & TP] Pindahkan karakter dalam mode slow-mo
        h.WalkSpeed = HIGH_WALKSPEED -- Set kecepatan tinggi
        c:SetPrimaryPartCFrame(tp_pos) 
        
        -- [TRICK 3: SPAM TP (Fall-back)] Ulangi 5 kali untuk memastikan posisi dikunci
        for i = 1, 5 do
             c:SetPrimaryPartCFrame(tp_pos)
             wait() -- Menunggu satu frame lokal (yang sekarang sangat lambat)
        end
        
        -- [TRICK 4: KEMBALI NORMAL] Kembalikan kecepatan waktu dan lari
        if TimeScale and TimeScale.PlaybackSpeed then 
            TimeScale.PlaybackSpeed = current_time_scale -- Kembali ke kecepatan normal
        end
        h.WalkSpeed = original_speed
        
        warn("TP: Berhasil Pindah V8.0 (Time-Warp)! Cek posisi Anda.")
    else
        warn("TP: Simpan posisi dulu!")
    end
end

-- Di dalam GUI Anda, GANTI Tombol TP2 (Go) dengan:
create_button("TP2 (Go)", 80, execute_tp_time_warp)
